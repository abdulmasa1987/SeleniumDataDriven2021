
*********************GIT************************************************************
Download git and install
Create account on git - https://github.com
open cmd prompt and type "git"

Project > Staging Env > Local Git Repository > Git Hub Account

git init -- Initialize local Rep - will create .git in Users
git status - shows shifted project to local git repository. In red means not shifted to staigng  (in geen means it is at staging) 
Register usernanme with Git - git config --global user.name "abdulmasa1987"
Register email with Git - git config --global user.email "abdul.cool987@gmail.com"
git add . --> Adds all the folder to staging
git add <foldername> --> Adds specific folder to staging
git commit -m "first commit" //add to local repsoitory
git remote add origin https://github.com/abdulmasa1987/SeleniumDataDrivenFramework2021.git   //location of git on report
git push -u origin master   //push on git

***********************Now  Connect Jenkins to github************************************


mvn exec:java // if I want to run the project on co  <plugin>
    <groupId>org.codehaus.mojo</groupId>
    <artifactId>exec-maven-plugin</artifactId>
    <executions>
        <execution>
            <id>my-execution</id>
            <phase>test</phase>
            <goals>
                <goal>java</goal>
            </goals>
        </execution>
    </executions>
    
    <configuration>
        <mainClass>runner.JSONRunner</mainClass>
            <classpathScope>test</classpathScope>
    </configuration>
</plugin>  mmand prompt using maven. This command basically means i have to execute the standalone java file with public static void main() in it